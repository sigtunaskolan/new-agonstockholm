import styled from "@emotion/styled";

const Container = styled("div")(() => {
  return {
    height: "100px",
    width: "90vw",
    marginBottom: "20px",
    display: "flex",
    alignItems: "center",
  };
});

export default Container;
